# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman
# Contributor: FranÃ§ois Charette <firmicus Î±Ï„ gmx Î´Î¿Ï„ net>

pkgname=perl-text-csv-xs
pkgver=0.62
pkgrel=1
pkgdesc="Fast 8bit clean version of Text::CSV"
arch=('i686' 'x86_64')
url="http://search.cpan.org/dist/Text-CSV_XS"
license=('GPL' 'PerlArtistic')
depends=('perl>=5.10.0')
options=('!emptydirs')
source=(http://search.cpan.org/CPAN/authors/id/H/HM/HMBRAND/Text-CSV_XS-$pkgver.tgz) 
md5sums=('e617de0fac0c4e392e2b713982a7c077')

build() {
  cd  $srcdir/Text-CSV_XS-$pkgver
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make install DESTDIR=$pkgdir || return 1
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
