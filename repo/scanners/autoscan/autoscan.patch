--- Makefile	2008-11-09 10:09:29.000000000 -0700
+++ ../src/autoscan-network-1.32/Makefile	2008-11-09 10:07:32.000000000 -0700
@@ -86,18 +86,18 @@
 	$(INSTALL) -c -m 644 usr/share/sounds/autoscan-network/* $(DESTDIR)/usr/share/sounds/autoscan-network/
 
 	@echo '[01;34m***  Gconf config...[00m'
-	export GCONF_CONFIG_SOURCE=$(GCONF);gconftool-2 --makefile-install-rule /usr/share/apps/autoscan-network/autoscan-network.schemas &>/dev/null
+	export GCONF_CONFIG_SOURCE=$(GCONF);gconftool-2 --makefile-install-rule $(DESTDIR)/usr/share/apps/autoscan-network/autoscan-network.schemas &>/dev/null
 	@echo '[01;34m*** Installing executable...[00m'
 	$(INSTALL) -c -m 755 bin/autoscan-network $(DESTDIR)/usr/bin/
-	@instance=`ps aux  | grep "[0-9] /usr/sbin/autoscan-network-daemon" | wc -l`; echo "Instance (autoscan-network-daemon) : $$instance"; if [ "$$instance" -eq 2 ]; then echo "kill Agent"; killall autoscan-network-daemon; fi
+#	@instance=`ps aux  | grep "[0-9] /usr/sbin/autoscan-network-daemon" | wc -l`; echo "Instance (autoscan-network-daemon) : $$instance"; if [ "$$instance" -eq 2 ]; then echo "kill Agent"; killall autoscan-network-daemon; fi
 	$(SHTOOL) mkdir -f -p -m 755 $(DESTDIR)/usr/sbin
-	$(SHTOOL) mkdir -f -p -m 755 $(DESTDIR)/etc/init.d
+#	$(SHTOOL) mkdir -f -p -m 755 $(DESTDIR)/etc/init.d
 	$(INSTALL) -c -m 755 -o root -g root bin/autoscan-network-daemon $(DESTDIR)/usr/sbin/
-	$(INSTALL) -c -m 755 -o root -g root 
+#	$(INSTALL) -c -m 755 -o root -g root 
 
-	@echo '[01;34m*** Installing Script...[00m'
+#	@echo '[01;34m*** Installing Script...[00m'
 	
-	/etc/init.d/autoscan-network start
+#	/etc/init.d/autoscan-network start
 
 	@echo '[01;34m*** Done installing.[00m'
 
